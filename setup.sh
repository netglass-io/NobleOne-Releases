#!/bin/bash
# NobleOne Device Setup Script (Phase 2)
# This script is served by Hub after device activation.
# It is NOT meant to be run directly — install.sh fetches and executes it.
#
# All configuration is passed via environment variables set by install.sh.
echo ""
echo "This script has been superseded by install.sh."
echo "Run install.sh instead — it handles the full provisioning process."
echo ""
exit 0
